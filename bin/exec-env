#!/usr/bin/env bash

if [ "$SBCL_HOME" = "" ]; then
  export SBCL_HOME="$ASDF_INSTALL_PATH/lib/sbcl"
else
  export SBCL_HOME="$SBCL_HOME"
fi

if [ "$QUICKLISP_HOME" = "" ]; then
  export QUICKLISP_HOME="$ASDF_INSTALL_PATH/quicklisp"
else
  export QUICKLISP_HOME="$QUICKLISP_HOME"
fi
