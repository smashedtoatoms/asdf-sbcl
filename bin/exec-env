#!/usr/bin/env bash

if [ "$SBCL_HOME" = "" ]; then
  export SBCL_HOME="$ASDF_INSTALL_PATH/lib/sbcl"
fi

if [ "$CLPM_CLIENT_PATH" = "" ]; then
  export CLPM_CLIENT_PATH="$ASDF_INSTALL_PATH/lib/clpm/client/"
fi
